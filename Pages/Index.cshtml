@page "/"
@page "/{Filter}"
@inject AppState state

<TodoInput OnNewTodo=@state.AddTodo />
<TodoList State=@state Filter=@Filter />
<TodoFooter State=@state Filter=@Filter />

@functions {
    public string Filter { get; set; }
}